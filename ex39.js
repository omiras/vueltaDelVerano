// ex39.js
// Reto final E: función esReservaLarga

function esReservaLarga(reserva) {
  // Completa aquí: devuelve true si noches >= 5
}

const reserva = {
  hotel: "Hotel Mar",
  noches: 5,
  precio: 65
};

console.log(esReservaLarga(reserva));
